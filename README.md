# 1. Initialize the repository
git init

# 2. Add all files
git add .

# 3. Create initial commit
git commit -m "Initial release of Rungle: Neon Invaders"

# 4. (Go to github.com and create a new repository named 'neon-invaders')
# 5. Link and Push
git remote add origin https://github.com/YOUR_USERNAME/neon-invaders.git
git branch -M main
git push -u origin main
